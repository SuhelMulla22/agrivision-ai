{
  "name": "agrivision-ai-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "backend": "cd backend && venv\\Scripts\\python -m uvicorn app.main:app --port 8000",
    "backend:reload": "cd backend && venv\\Scripts\\python -m uvicorn app.main:app --reload --port 8000"
  }
}
